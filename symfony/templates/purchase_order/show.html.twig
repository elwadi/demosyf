{% extends 'base.html.twig' %}

{% block title %}PurchaseOrder{% endblock %}

{% block body %}
    <h1>PurchaseOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purchase_order.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ purchase_order.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_purchase_order_index') }}">back to list</a>

    <a href="{{ path('app_purchase_order_edit', {'id': purchase_order.id}) }}">edit</a>

    {{ include('purchase_order/_delete_form.html.twig') }}
{% endblock %}
